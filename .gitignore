# Dependencies
node_modules

# Build output
dist

# Local env files (keep secrets/webhooks out of git)
.env
.env.local
.env.*.local

# OS / editor
.DS_Store

# Tooling
.playwright-mcp
.gstack/

# Full-res image backups (created during image optimization; keep local only)
public/_originals/
.vercel
